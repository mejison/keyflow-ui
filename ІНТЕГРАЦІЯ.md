# KeyFlow UI - Інтеграція з API

## ✅ Що було зроблено

### 1. Створено API сервіс
- **Файл**: `src/services/api.ts`
- Централізований сервіс для всіх API запитів
- Використовує Axios з автоматичним управлінням токенами
- Автоматична обробка помилок (401, CORS тощо)

### 2. Оновлено Auth Store
- **Файл**: `src/stores/auth.ts`
- Повна інтеграція з Laravel API
- Методи: login, signup, logout, OAuth, password reset
- Обробка помилок та loading станів

### 3. Оновлено сторінки

#### Login (`src/pages/Login.vue`)
- ✅ Вхід через email/password
- ✅ OAuth (Google, GitHub)
- ✅ Обробка помилок
- ✅ Loading стани

#### Signup (`src/pages/Signup.vue`)
- ✅ Реєстрація нового користувача
- ✅ Підтвердження пароля
- ✅ OAuth реєстрація
- ✅ Валідація на клієнті

#### Forgot Password (`src/pages/ForgotPassword.vue`)
- ✅ Запит на скидання пароля
- ✅ Відправка email
- ✅ Стан успіху

#### Reset Password (`src/pages/ResetPassword.vue`)
- ✅ **НОВА СТОРІНКА**
- ✅ Форма для встановлення нового пароля
- ✅ Отримання токену з email посилання
- ✅ Підтвердження пароля

#### Profile (`src/pages/Profile.vue`)
- ✅ Автоматичне завантаження даних користувача
- ✅ Logout функціонал
- ✅ Захищений роут

#### Auth Callback (`src/pages/AuthCallback.vue`)
- ✅ Обробка OAuth callback
- ✅ Інтеграція з Laravel API endpoints

### 4. Налаштування
- ✅ Оновлено `.env` та `.env.example`
- ✅ API_BASE_URL: `http://localhost:8000`
- ✅ OAuth налаштування на бекенді (не потрібні в фронтенді)

## 📋 API Endpoints (відповідають Postman колекції)

### Автентифікація
```
POST /api/v1/auth/register       - Реєстрація
POST /api/v1/auth/login          - Вхід
POST /api/v1/auth/logout         - Вихід
GET  /api/v1/auth/me             - Отримати поточного користувача
POST /api/v1/auth/forgot-password - Запит на скидання пароля
POST /api/v1/auth/reset-password  - Скинути пароль
```

### OAuth
```
GET /api/v1/auth/social/google          - Отримати Google redirect URL
GET /api/v1/auth/social/google/callback - Google callback
GET /api/v1/auth/social/github          - Отримати GitHub redirect URL
GET /api/v1/auth/social/github/callback - GitHub callback
```

## 🚀 Як запустити

### 1. Встановити залежності
```bash
npm install
```

### 2. Налаштувати .env
Файл `.env` вже налаштований з правильним API URL:
```env
VITE_API_BASE_URL=http://localhost:8000
```

### 3. Запустити dev сервер
```bash
npm run dev
```

### 4. Переконайтеся що Laravel backend працює
Backend повинен працювати на `http://localhost:8000`

## 🔐 OAuth Flow

### Як працює OAuth:
1. Користувач натискає "Continue with Google/GitHub"
2. Frontend перенаправляє напряму на Laravel endpoint `/api/v1/auth/social/{provider}`
3. Laravel перенаправляє на Google/GitHub для авторизації
4. Після авторизації, OAuth provider повертається на Laravel callback
5. **Laravel створює токен і перенаправляє на frontend** з токеном: `/auth/callback/{provider}?token={access_token}`
6. Frontend зберігає токен, завантажує дані користувача
7. Користувач авторизований і перенаправлений на профіль

### Важливо для Backend:
- Laravel **ПОВИНЕН** перенаправляти на frontend з токеном, а не повертати JSON
- Callback URL: `{FRONTEND_URL}/auth/callback/{provider}?token={access_token}`
- При помилці: `{FRONTEND_URL}/auth/callback/{provider}?error={message}`

### 📋 Інструкція для налаштування Laravel

Дивіться файл **`LARAVEL_OAUTH_SETUP.md`** для детальної інструкції як налаштувати OAuth callback у Laravel контролері.

**Коротко:** У вашому OAuth callback контролері замініть `return response()->json(...)` на:
```php
$frontendUrl = env('FRONTEND_URL', 'http://localhost:5173');
return Redirect::to("{$frontendUrl}/auth/callback/{$provider}?token={$token}");
```

### Що змінилось на Frontend:
- OAuth тепер очікує токен напряму в URL: `?token={access_token}`
- Автоматичне збереження токену та завантаження даних користувача
- Підтримка як нового flow (з токеном), так і старого (з code) для зворотної сумісності

## 🧪 Тестування

### Email Authentication
1. Відкрийте `/signup`
2. Створіть акаунт
3. Увійдіть через `/login`
4. Перевірте профіль

### Password Reset
1. Відкрийте `/forgot-password`
2. Введіть email
3. Перевірте email (лист від Laravel)
4. Перейдіть за посиланням
5. Встановіть новий пароль

### OAuth
1. Натисніть "Continue with Google" або "Continue with GitHub"
2. Авторизуйтесь
3. Повинні бути автоматично увійдені

## 📦 Встановлені пакети
- `axios` - для HTTP запитів
- `pinia` - state management (вже був)
- `vue-router` - навігація (вже був)

## 📝 Типи помилок

### CORS
Переконайтеся що Laravel налаштований для `http://localhost:5173`

### 401 Unauthorized
- Перевірте токен
- Токен міг застаріти
- Переавторизуйтесь

### 404 Not Found
- Перевірте що бекенд запущений
- Перевірте `VITE_API_BASE_URL` в `.env`

## 📖 Документація

Детальна англомовна документація: `API_INTEGRATION.md`

## ✨ Особливості

- **TypeScript** - повна типізація
- **Автоматичне управління токенами** - токени зберігаються в localStorage
- **Обробка помилок** - всі помилки відображаються користувачу
- **Loading стани** - кнопки блокуються під час запитів
- **Валідація** - на клієнті та сервері
- **Захищені роути** - автоматичний редірект на login
- **Персистентна сесія** - токен зберігається між перезавантаженнями

## 🎉 Готово до використання!

Всі функції з Postman колекції інтегровані та готові до роботи.
